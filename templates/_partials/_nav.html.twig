<nav class="navbar navbar-expand-lg navbar-dark bg-danger shadow-sm">
	<div class="container-fluid">
		<a class="navbar-brand fw-bold d-flex align-items-center" href="{{ path('main') }}">
			<i class="bi bi-shop me-2 fs-4"></i>
			<span>E-Commerce</span>
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link {{ app.request.get('_route') == 'main' ? 'active' : '' }}" href="{{ path('main') }}">
						<i class="bi bi-house-door"></i> Accueil
					</a>
				</li>
				{% if app.user and not is_granted('ROLE_PRODUCT_ADMIN') %}
					<li class="nav-item">
						<a class="nav-link {{ app.request.get('_route') == 'cart_index' ? 'active' : '' }}" href="{{ path('cart_index') }}">
							<i class="bi bi-cart"></i> Panier
						</a>
					</li>
				{% endif %}
			</ul>
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
				{% if app.user %}
					{% if is_granted('ROLE_PRODUCT_ADMIN') %}
						<li class="nav-item">
							<a class="nav-link {{ app.request.get('_route') starts with 'admin_' ? 'active' : '' }}" href="{{ path('admin_index') }}">
								<i class="bi bi-speedometer2"></i> Administration
							</a>
						</li>
					{% endif %}
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							<i class="bi bi-person-circle me-1"></i>
							<span>{{ app.user.firstname }} {{ app.user.lastname }}</span>
						</a>
						<ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="userDropdown">
							<li>
								<h6 class="dropdown-header">
									<i class="bi bi-person"></i> Mon compte
								</h6>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('profile_index') }}">
									<i class="bi bi-person"></i> Profil
								</a>
							</li>
							{% if not is_granted('ROLE_PRODUCT_ADMIN') %}
								<li>
									<a class="dropdown-item" href="{{ path('profile_orders') }}">
										<i class="bi bi-bag"></i> Mes commandes
									</a>
								</li>
							{% endif %}
							<li><hr class="dropdown-divider"></li>
							<li>
								<a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
									<i class="bi bi-box-arrow-right"></i> Déconnexion
								</a>
							</li>
						</ul>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link {{ app.request.get('_route') == 'app_register' ? 'active' : '' }}" href="{{ path('app_register') }}">
							<i class="bi bi-person-plus"></i> S'inscrire
						</a>
					</li>
					<li class="nav-item">
						<a class="btn btn-light text-danger ms-2 {{ app.request.get('_route') == 'app_login' ? 'active' : '' }}" href="{{ path('app_login') }}">
							<i class="bi bi-box-arrow-in-right"></i> Connexion
						</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
