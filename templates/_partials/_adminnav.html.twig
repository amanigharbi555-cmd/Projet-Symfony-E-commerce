<aside class="col-3 border border-light bg-white p-3 shadow-sm">
    <div class="mb-4">
        <a href="{{ path('admin_index') }}" class="text-decoration-none">
            <h4 class="text-danger mb-0">
                <i class="bi bi-speedometer2"></i> Dashboard
            </h4>
        </a>
    </div>
    
    <article class="card mb-3 shadow-sm">
        <div class="card-header bg-danger text-white">
            <i class="bi bi-tags"></i> Catégories
        </div>
        <div class="card-body p-2">
            <a href="{{ path('admin_categories_index') }}" class="btn btn-sm btn-outline-danger w-100 mb-2 text-start">
                <i class="bi bi-list-ul"></i> Liste des catégories
            </a>
            <a href="{{ path('admin_categories_add') }}" class="btn btn-sm btn-outline-success w-100 text-start">
                <i class="bi bi-plus-circle"></i> Ajouter une catégorie
            </a>
        </div>
    </article>
    
    <article class="card mb-3 shadow-sm">
        <div class="card-header bg-success text-white">
            <i class="bi bi-box-seam"></i> Produits
        </div>
        <div class="card-body p-2">
            <a href="{{ path('admin_products_index') }}" class="btn btn-sm btn-outline-success w-100 mb-2 text-start">
                <i class="bi bi-list-ul"></i> Liste des produits
            </a>
            <a href="{{ path('admin_products_add') }}" class="btn btn-sm btn-outline-success w-100 text-start">
                <i class="bi bi-plus-circle"></i> Ajouter un produit
            </a>
        </div>
    </article>
    
    <article class="card mb-3 shadow-sm">
        <div class="card-header bg-info text-white">
            <i class="bi bi-people"></i> Utilisateurs
        </div>
        <div class="card-body p-2">
            <a href="{{ path('admin_users_index') }}" class="btn btn-sm btn-outline-info w-100 text-start">
                <i class="bi bi-list-ul"></i> Liste des utilisateurs
            </a>
        </div>
    </article>
    
    <div class="mt-4">
        <a href="{{ path('main') }}" class="btn btn-outline-secondary w-100">
            <i class="bi bi-arrow-left"></i> Retour au site
        </a>
    </div>
</aside>