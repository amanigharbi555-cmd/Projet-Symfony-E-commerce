{{ form_start(categoryForm, {'attr': {'class': 'needs-validation', 'novalidate': true}}) }}
    <div class="row">
        <div class="col-md-8">
            <div class="card shadow-sm mb-3">
                <div class="card-header bg-danger text-white">
                    <i class="bi bi-info-circle"></i> Informations de la catégorie
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        {{ form_label(categoryForm.name) }}
                        {{ form_widget(categoryForm.name) }}
                        {{ form_errors(categoryForm.name) }}
                    </div>
                    <div class="mb-3">
                        {{ form_label(categoryForm.parent) }}
                        {{ form_widget(categoryForm.parent) }}
                        <small class="form-text text-muted">
                            <i class="bi bi-info-circle"></i> Laissez vide pour créer une catégorie principale
                        </small>
                        {{ form_errors(categoryForm.parent) }}
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card shadow-sm mb-3">
                <div class="card-header bg-secondary text-white">
                    <i class="bi bi-sliders"></i> Paramètres
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        {{ form_label(categoryForm.categoryOrder) }}
                        {{ form_widget(categoryForm.categoryOrder) }}
                        <small class="form-text text-muted">
                            <i class="bi bi-info-circle"></i> Ordre d'affichage (0 par défaut)
                        </small>
                        {{ form_errors(categoryForm.categoryOrder) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="d-flex gap-2">
        <button type="submit" class="btn btn-danger">
            <i class="bi bi-check-circle"></i> Enregistrer
        </button>
        <a href="{{ path('admin_categories_index') }}" class="btn btn-secondary">
            <i class="bi bi-x-circle"></i> Annuler
        </a>
    </div>
{{ form_end(categoryForm) }}

